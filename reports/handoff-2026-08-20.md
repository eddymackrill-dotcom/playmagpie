# PlayMagpie .com: full project handoff

Written 20 August 2026, late evening. Audience: a new assistant instance
with zero prior context. Scope: the playmagpie.com repo ONLY. The .UK is
a separate project behind an entity firewall; that sentence is the
entire extent of what this document says about it.

Method: every claim below is derived from repo evidence (STATE.md,
RUNBOOK.md, CLAUDE.md, the decisions log, git history, the sitemap,
reports/ including tonight's 19c0d95 additions, and logged live-site
checks). Session memory was not used as a source. Where evidence
conflicts, the contradiction is reported, not silently resolved.
Conclusions are labelled fact / hypothesis / guess. All dates 2026
unless stated.

## 1. Operator and setup

- Owner: Eddy Mackrill (git author), publishing pseudonymously; the
  site brand is PlayMagpie, X account @MagpieGG. UK-based (GSC GBR
  exclusion rule exists because the owner's own browsing pollutes GBR
  rows).
- Environment: Windows 11, repo at C:\projects\aff_website, deploys via
  Vercel on push to master (push IS deploy), GitHub Actions carries the
  Bing submission workflow. gh CLI is authenticated. GSC access via the
  gsc MCP plus a documented scratchpad-token fallback
  (C:\Users\Jen\gsc-config\, RUNBOOK gotchas section). The owner does
  not run git, GitHub, or Vercel by hand.
- Working model (fact, visible throughout the decisions log): a chat
  assistant drafts session prompts, the owner pastes them into Claude
  Code, and Claude Code executes. SHOW-ME STOPS in a session brief are
  HARD GATES: they override the standing autonomy rule for that ship
  (precedent recorded in reports/au-senate-execution-runbook-2026-08-17.md
  and honoured in every August ship). Between stops, the 2026-08-01
  autonomy rule governs: decide, act, report; only a three-part
  emergency bar stops a session (illegal / immediate block-delist risk /
  direct reader harm).
- Owner glances (WMT, GA4 UI, dashboards Claude Code cannot reach) are
  a legitimate, named category of manual step; each must be requested
  precisely, with the URL and what a bad result looks like.

## 2. The site, the June suppression, and what recovery looks like

The site is an independent crypto-casino review affiliate site
(~74 URLs, 9 operators in lib/casinos.ts), targeting Tier 1 English
markets plus DE/NL/Nordics, explicitly not the UK. Editorial thesis:
honest, specific, primary-sourced content beats generic affiliate spin.
Verify-or-omit governs every written figure, always, and is untouched by
every rule relaxation on record.

THE SUPPRESSION (fact): the Google June 2026 spam update (rolled out
24-26 Jun) put the site under a SERVING-layer suppression: impressions
went from ~749/day (25 Jun) to zero (27 Jun onward) and have stayed
effectively zero since (the recovery-log streak has never left 0/7).
Crawling and indexing stayed normal: the 07-07 census
(lib/index-census-2026-07-07.md) found 82 of 87 sitemap URLs still
indexed, with the only quality-rejection drops being 3 withdrawal-intent
pages, and FAQ rich results revoked site-wide. The operative reading in
STATE: a site-level trust/spam classification at serving, not
cluster-level content rejection.

CONTRADICTION ON RECORD (report, do not resolve): tonight's RUNBOOK
post-rollout protocol says "97 per cent of pages stayed indexed"; the
census the claim descends from says 82/87, which is 94 per cent. The
census is the primary source. The protocol line came in via the 20 Aug
session brief and should be corrected to the census figure at the next
RUNBOOK touch.

WHAT RECOVERY LOOKS LIKE (fact, CLAUDE.md velocity-freeze section):
recovery is confirmed when GSC shows 7 or more consecutive days of
non-zero site-wide impressions (dataState=all, raw count). At
confirmation the velocity freeze lifts, publishing restarts at reduced
cadence (max 3 pages/week for 4 weeks), every batch preceded by queued
go/no-go reads, and the event goes in the recovery log and decisions
log. The daily serving check is the instrument watching for this.

THE FLOOR while suppressed (fact, STATE): the site's working identity is
an assistant-cited reference site. Three measured layers: Bing serving
(confirmed sustained since 25 Jul), Copilot citations (Bing WMT AI
Performance, BETA), assistant sessions (GA4, the site's #2 channel).
Bing's index feeds ChatGPT search and Copilot, so Bing health is
load-bearing, not a side experiment. Affiliate revenue evidence to date
(owner-supplied 08-01): BitStarz 4 FTDs / 500 GGR off ~79 sessions per
28 days; which page or channel produced them was unknowable before the
affiliate_click instrumentation (18 Aug).

## 3. Operating rules, each with its origin

Read CLAUDE.md and RUNBOOK.md in full before acting; this list is the
orientation layer, not a substitute.

- AUTONOMY RULE (owner decision 01 Aug, .com only, permanent): nothing
  stops a session except the three-part emergency bar. Eight named stop
  conditions were removed. Judgement calls are taken and flagged in the
  report. Does NOT relax verify-or-omit. Session-brief show-me stops
  override it per ship.
- ACCURACY RULE (owner decision 01 Aug, .com only): accuracy corrections
  are not priority work; no sweeps, no stopping for inaccuracies; only
  the emergency bar forces a fix. Noticed items get one line in the
  final report.
- VELOCITY (owner decision 01 Aug): 4 published pages per calendar
  month, ceiling not target, session cap 2, all clusters eligible; the
  per-page scaled-content risk assessment replaced the cluster filter.
  PAGE COUNTING (precedent, 08-02 count-discrepancy entry): the count of
  record is the repo's verifiable evidence (commits and the sitemap URL
  set delta), not recollection; when the owner's stated count and the
  repo differed, the repo count was used and the discrepancy flagged.
- SETTLED LIST (never re-raise; each logged with the counter-argument
  preserved): the accuracy-rule counter-argument (01 Aug); the velocity
  counter-argument (01 Aug); the citation-vs-conversion tension
  (Cloudbet cited/zero FTDs vs BitStarz uncited/4 FTDs, 01 Aug, do not
  use either number against the other); the post-rollout sequencing
  decision (20 Aug: ship 2c/2d first, then one Request Indexing
  campaign; assistant alternative heard and overruled).
- PROPAGATION (standing): the auto Bing workflow diffs URL SETS only, so
  content changes to existing pages NEVER self-propagate;
  workflow_dispatch (or gh workflow run) is the only route, 10-URL cap,
  and every content commit must state its propagation step explicitly.
  New URLs self-submit on push.
- VERIFICATION STANDARD (owner, 28 Jul, reaffirmed 01 Aug): an HTTP 200
  plus quota decrement is submitted-pending-confirmation, never
  verified; only visible appearance in WMT URL Submission reporting
  closes a ledger entry. The WMT export shows one row per URL, latest
  submission only (fact: 53 rows, no duplicates; latest-per-URL is a
  supported hypothesis via two multi-dispatch cases).
- QUOTA-NOISE RULE (17 Aug, RUNBOOK): monthly Bing quota readings are
  noise (400 / 3098 / 1500 / 1399 / 1200 across five reads, no pattern);
  plan against the stable daily 100 only.
- POST-31-AUG RULE (17 Aug, amended proactive 17 Aug evening): the Bing
  channel already uses the api.svc/json binding, which survives the
  31 Aug SOAP/POX retirement (fact, documented in
  reports/bing-ledger-reconciliation-2026-08-17.md); the first dispatch
  after 31 Aug stays pending until seen in a WMT export, because a
  post-retirement failure would be silent (200-but-nowhere).
- rel="sponsored" DEPENDENCY (18 Aug, standing): every affiliate link
  MUST carry rel="sponsored". The affiliate_click tracker keys off that
  attribute; a bare CTA navigates fine but silently vanishes from
  measurement. Enforced at review on any CTA-touching change.
- MEASUREMENT-COMMITS RULE (18 Aug, CLAUDE.md): analytics-only commits
  with byte-verified zero rendered-content change never bump sitemap
  lastmod and are never Bing-dispatched.
- INVERTED SCAFFOLD GATE (17 Aug): production builds EXCLUDE any
  legal-page block carrying a REPORT-PENDING token (local builds render
  it); a token outside the excludable arrays fails the build. The
  execution-day consequence: a forgotten token silently drops its
  section, so the success criterion for any fill on that page is
  POSITIVE visual confirmation on the live page before dispatch (passed
  twice: 18 Aug, 20 Aug).
- Em-dash-zero in all user-facing content and repo docs; banned-phrase
  list; per-page honest lastmod (bump only on real content events);
  never git add -A; subagents never push; explicit-path staging;
  10-URL dispatch cap is a tripwire, not a throttle.

## 4. Chronology, 28 Jul to 20 Aug (from the decisions log and git history)

- 28 Jul: /guides/why-is-my-crypto-casino-withdrawal-pending shipped
  (July's single slot under the then-current rule). IndexNow closed
  unsolved and removed; Bing Webmaster URL Submission API adopted
  (workflow submit-bing.yml, URL-set diff + manual dispatch).
- 01 Aug: the big rule day (autonomy, accuracy, velocity 4/month, all
  owner decisions with counter-arguments logged). Correction sweeps run
  earlier that day produced 17 corrected URLs, dispatched in two runs
  (9+8, all HTTP 200, decrement observed). BC.Game and 7Bit no-KYC
  absolutism claims failed owner live-terms verification and were
  corrected site-wide. August slate approved. Two pages shipped:
  /guides/large-crypto-casino-withdrawals, /compare/cloudbet-vs-roobet.
  First Bing AI Performance grounding read (82 citations, all four
  queries operator-branded). First affiliate outcome data logged.
- 02 Aug: /reviews/bitstarz/bonus shipped (August page 3; the count
  discrepancy with the owner's message flagged and resolved to the repo
  count). Owner dispatched reviews/bitstarz; bonus/free-spins intended
  (its 08-02 submission is indeterminate in the export; covered 08-08).
- 03 Aug: the scheduled monthly structural audit DID NOT RUN (fact by
  absence: no decisions entry, no census file; discovered 20 Aug).
  Carried scope still open: 73-URL census diff, 5-operator catalogue
  check, WMT query-list capture, /no-kyc-casinos page-level decision.
- 07 Aug: Batch 1 de-templating shipped (10 country pages) with the
  per-page-lastmod system (which de facto resolved the sitemap-lastmod
  agenda item). GSC credential-wipe problem (a daily ~17:49 job) worked
  around via the safe-path token dir on 08 Aug.
- 08 Aug: Batch 2a shipped (7 bonus pages) after a three-gate morning.
- 10 Aug: recrawl gate checked: ZERO Google recrawls on all 17 watched
  URLs; no-recrawl branch fired; owner override shipped Batch 2b alone
  (8 crypto/game pages); 2c/2d committed as approved drafts
  (reports/batch2bcd-drafts.md). Citation spike (86 then 162/day vs
  July ceiling ~40) attributed to DEMAND on untouched pages, not the
  batches; australia/legal regression floor pinned at 23 (the 08-07
  baseline), later reset (see 18 Aug).
- 11 Aug: additive Gambling Reform Bill update shipped to
  /country/australia/legal (owner-approved; byte-verified additive).
  Regulatory programme launched under Option D: Canada/Alberta additive
  update, two trackers published (/tracker/prediction-markets-legality,
  /tracker/us-sweepstakes-casinos-by-state), portfolio instrumentation
  opened, radar merged into the RUNBOOK. Both propagation routes
  confirmed working. Forward calendar consolidated.
- 16 Aug: pre-flight session built the Senate-report scaffold, fenced
  with REPORT-PENDING tokens and a build-BLOCKING gate; left
  uncommitted in the working tree (session committed nothing).
- 17 Aug: no report tabled (abort branch 1; owner checked the committee
  page, "Submissions Closed"). Gate INVERTED from block-the-build to
  exclude-the-section, verified three ways, first live proof on deploy.
  Sentence-level VPN-caveat edit shipped, with a location correction:
  the sentence lives on the /country/australia HUB, not the legal page.
  Bing SOAP/POX retirement checked: channel already on the json binding,
  no change. Submission ledger reconciled against the owner's WMT export
  (53 of 54 URLs CONFIRMED; the one absentee an apex-host test
  artifact); the since-28-Jul pending ledger CLOSED. 17 Aug AI reads
  logged (breadth 0-for-3 at day 6; withdrawal guide 102 to 168, #3
  asset; Cloudbet frozen two reads; regime change dated 07 Aug, peak 283
  on 14 Aug, max 5 cited pages/day). Checkpoint A floor reset to a
  trailing-7-day basis. Session cap and checkpoint inputs consolidated.
- 18 Aug: report tabled 17 Aug (Monday night AEST). Entry 1 (committee
  report, filled solely from the report PDF, para-anchored) and Entry 2
  (government amendments, news-sourced) shipped same-cycle; first
  inverted-gate execution passed positive visual confirmation;
  dispatched HTTP 200. The report PDF was NOT committed (CC BY-NC-ND,
  public repo; gitignored by name). GA4 affiliate_click tracker shipped
  (one delegated listener on a[rel~="sponsored"], zero DOM change,
  headless-browser verified, negative control passed). GA4 28-day read
  logged: AI Assistant channel FLAT at 21 sessions against a ~20x
  citation rise; conclusion (fact at current scale): a Copilot citation
  is an impression, not a referral, roughly 1 session per ~80 citations.
- 20 Aug (morning): full state briefing surfaced two headline facts:
  the August 2026 spam update began 18 Aug (~09:27 PT) and is the
  reassessment event; the Gambling Reform Bill passed BOTH houses
  (House 18 Aug with 26 government + 45 opposition amendments agreed;
  Senate 19 Aug, no further amendment). Also surfaced: the weekly
  check + update watch had lapsed since 25 Jul, the radar had never
  run, and the 08-03 audit never happened.
- 20 Aug (day): owner set the affiliate_click key-event flag. Entry 3
  (Senate passage) shipped from the parliamentary record with news
  colour attributed (ABC: 96 to 9, Conaghan and Wallace crossed the
  floor); commencement dates OMITTED under verify-or-omit because
  parlinfo 403s the as-passed text; dispatched HTTP 200 with the
  decrement observed at exactly 1; the open ledger entry rolled to
  expect an 08-20 WMT row. Daily serving check protocol running.
- 20 Aug (evening, commit 19c0d95): post-rollout protocol written into
  the RUNBOOK (trigger: dashboard shows the update COMPLETE; step 1:
  2c/2d deploy with staleness re-verify; step 2: Request Indexing
  campaign the following morning); sequencing owner-decided, settled;
  the ranked campaign list built and crawl-verified
  (reports/request-indexing-list-2026-08.md).

## 5. Current state of every system (as of 20 Aug, late evening)

SERVING (Google): suppressed; streak 0/7; by-day impressions zero
through 19 Aug (single stray 08-07; last two days provisional). THE
AUGUST 2026 SPAM UPDATE IS MID-ROLLOUT since 18 Aug and is the
reassessment event the recovery plan has waited for since July. Daily
serving + rollout-status check is mandatory until completion, and
UPDATE WATCH stays daily thereafter until serving resumes or the owner
rules otherwise (the tripwire lapsed three times; the 18 Aug rollout
was caught two days late).

DE-TEMPLATING: 25 of 39 URLs deployed (10 + 7 + 8). Google recrawls of
changed pages since 07 Aug: ZERO (fact, re-verified 20 Aug on all three
Checkpoint B sentinels). 2c/2d: 12 deployable pages (6 compare + 6
statics; the historic "remaining 14" counted the two approved
exclusions /high-roller-casinos and /bnb-crypto-casinos), committed as
approved drafts, HELD for the rollout-completion trigger under the
owner's settled sequencing decision (a). The reviews batch is gated on
a clean Checkpoint A and unscoped. The STOP branch for the checkpoint
(zero recrawls = stop and diagnose) is owner-approved and executes at
the 21 Aug session, not before.

POST-ROLLOUT PROTOCOL + CAMPAIGN LIST: RUNBOOK section of 20 Aug; list
at reports/request-indexing-list-2026-08.md, crawl-verified 20 Aug via
44 single-URL inspections: 31 URLs requestable now (VERIFIED-STALE),
12 more at 2c/2d ship, 3 excluded (ALREADY-CRAWLED: /methodology,
/about, /high-roller-casinos). Headline finding: Google last fetched
/country/australia/legal on 20 JUNE, before all four dated entries on
the site's top AI asset (changed through 20 Aug). Two URLs have never
been crawled at all (/bonus/free-spins; /compare/cloudbet-vs-roobet,
published 01 Aug). The campaign's fetch-response pattern doubles as the
STOP-branch diagnosis.

BING CHANNEL: healthy and verified end to end. The endpoint is the
api.svc/json binding, which is the 31 Aug retirement's migration
target, not its victim (fact, evidenced 17 Aug). The submission ledger
was reconciled and closed 17 Aug against the owner's WMT export.
Exactly ONE ledger entry is open: /country/australia/legal, dispatched
18 Aug and re-dispatched 20 Aug (HTTP 200, decrement observed 1), so
under the latest-per-URL export behaviour the expected WMT row is
08-20; it closes at the next owner WMT glance (input to the 21 Aug
session).

AI CITATIONS (Bing WMT AI Performance, BETA): repo-recorded facts: the
regime change began 07 Aug; the 17 Aug read logged peak 283 citations
on 14 Aug and a maximum of 5 distinct cited pages on any single day;
australia/legal at 811 to 957 cumulative (+146/day) PRE-catalyst;
breadth (two trackers + canada/legal) 0-for-3 at day 6; the withdrawal
guide 102 to 168, the #3 asset; Cloudbet frozen at 111 two consecutive
reads (branded demand stopped, fact). CONTRADICTION ON RECORD: the
20 Aug handoff brief references a peak of 302, two clusters, a
30-day-window caveat, ~3-day reporting lag, and AI exports delivered
20 Aug with a catalyst read pending ~23 Aug; NONE of that is in the
repo yet (the portfolio d7 columns are still empty). Treat those as
owner-asserted pending transcription at the 21 Aug session; where they
conflict with the repo (283 vs 302), the repo figure is the recorded
one until a newer export is transcribed. The Senate news cycle
(17-19 Aug) is a natural experiment on the Australia page's citation
rate; the catalyst read is pending fresh exports.

GA4 / MEASUREMENT: affiliate_click live since 18 Aug (delegated
tracker on rel="sponsored" links; params operator, page_path via the
native dp dimension, link_url, link_text; zero DOM change,
byte-verified). Key-event flag SET by the owner 20 Aug; Key events
accumulate from that date. One owner test click logged for discount:
BitStarz, 18 Aug morning UK. The settled analytical finding (18 Aug,
fact at current scale): citations are impressions, not referrals
(~1 session per ~80 citations); the channels producing humans are Bing
web search and whatever feeds BitStarz, which affiliate_click exists
to identify. The next GA4 28-day pull reads three legs
(assistant-channel sessions, affiliate_click by page and channel, the
discounted test click) and is a September-slate input.

THE AUSTRALIA PAGE (/country/australia/legal): the site's top AI asset
and its most complete editorial artifact: frozen original body, frozen
11 Aug bill update, then the three-entry Senate record (Entry 1
committee report 17 Aug, para-anchored to the report PDF; Entry 2
government amendments 18 Aug, news-attributed; Entry 3 passage 19 Aug,
parliamentary-record-anchored, shipped 20 Aug). PENDING: royal assent,
a one-line owner-triggered addendum that also carries the commencement
dates from the Act as made; the dates were DELIBERATELY OMITTED from
Entry 3 because parlinfo 403s the as-passed text and verify-or-omit
forbids carrying as-introduced figures forward. Every future fill on
this page requires positive visual confirmation live before dispatch
(inverted-gate rule).

REGULATORY PROGRAMME: two trackers + the Canada and Australia legal
pages live; portfolio instrumentation open but its d7 columns are
UNFILLED (the ~18 Aug first reads were never transcribed); the weekly
radar has NEVER RUN (no reports/regulatory-radar.md exists). The WA
stay ruling has LANDED (stay denied, injunction stands, geofence
deadlines reported 19 Aug and 2 Sep), so the prediction-markets
tracker's reviewBy trigger fired early: its first live update is due,
verify against the CourtListener docket at execution (21 Aug list).

## 6. Open items, each with its exact unlock

Cross-checked against the decisions log 20 Aug; nothing listed here is
closed.

OWNER-GATED:
- WMT URL Submission glance: closes the one open ledger entry
  (australia/legal, expect an 08-20 row). Unlock: the glance itself
  (21 Aug input 4).
- Fresh AI Performance export(s) with daily series: unlocks the
  Checkpoint A floors (trailing-7-day), the breadth verdict data, and
  the Senate-catalyst read; also back-fills the portfolio d7 columns.
  Note the untranscribed 20 Aug brief figures (see section 5).
- GA4 glance now the key-event flag is set (21 Aug input).
- Roobet three-clause glance (paragraph 10.8 fee boundary, the
  $200,000/day cap wording, the weekend-processing clause): unlocks
  /reviews/roobet/withdrawal, the last open August slot (11 days).
- Royal assent flag: unlocks the one-line Entry 3 addendum with
  commencement dates from the Act as made.
- Owner test-click discounts on any future GA4 test activity (standing).

MACHINE-SIDE, GATED ON TRIGGERS:
- 2c/2d ship: trigger = dashboard shows the August spam update
  COMPLETE (post-rollout protocol step 1; staleness re-verify
  mandatory; show-me stop).
- Request Indexing campaign: trigger = the morning after 2c/2d ships
  (protocol step 2; owner mechanics, ~10/day, list file is the log).
- STOP-branch execution and diagnosis: the 21 Aug checkpoint session
  (owner already approved the branch).
- Prediction-markets tracker update: trigger fired (WA ruling landed);
  execute from primary sources, 21 Aug list.
- KYC-process guide: reopen condition unchanged and UNMET
  (primary-sourced verification-process data for at least two
  operators).
- NZ legal page: plan ~20 Oct, build early November, live before the
  1 Dec deadline.
- Freeze lift: 7 consecutive non-zero GSC days (see section 2).
- Monthly audit: the 03 Aug audit lapsed entirely; its scope (census
  diff, catalogue check, WMT query list, /no-kyc-casinos decision)
  needs a disposition, plausibly folded into 7 Sep unless serving
  resumes first. UNDECIDED; raise at the 21 Aug session.

CALENDAR / WATCH:
- Rollout completion: daily watch, expected within days of 18 Aug.
- 22-24 Aug: Batch 2a/2b crawl watches close.
- 25 Aug: prediction-markets reviewBy backstop (superseded by the
  landed ruling but still the dated line).
- 31 Aug: Bing SOAP/POX retirement; no action expected; first
  post-31-Aug dispatch stays pending until seen in a WMT export.
- 13 Oct Alberta grey-market deadline; 1 Nov Oklahoma SB 1589; 1 Dec
  NZ licensing deadline (NZ hub reframe tripwire).
- Possible core update after this spam update (hypothesis): UPDATE
  WATCH stays daily regardless.

SEPTEMBER SLATE: settled by evidence: citation-magnet pages cannot be
justified on session expectations (the flat-sessions finding); the
withdrawal/KYC process cluster is the compounding performer. Conditional
on the 21 Aug breadth verdict: whether the regulatory breadth programme
is retired (0-for-3 at day 10 retires it, per the written rule).
Undecided: the actual September page list and slot allocation; whether
Roobet withdrawal lands in August or slides; everything subordinate to
the spam-update outcome, which can rewrite the premise in either
direction.

## 7. Immediate next action: the 21 Aug Checkpoints A+B session (tomorrow)

Full scope, already defined in the RUNBOOK (weekly section 2b + the
consolidated five-input list of 18 Aug):
1. Breadth verdict: trackers + canada/legal at day 10; zero across all
   three retires the breadth programme and reweights September toward
   the process cluster (exact rule as written in the RUNBOOK).
2. Checkpoint A vs reports/bing-baseline-2026-08-07/ under the
   crawl-date gate, regression floors on the TRAILING-7-DAY basis (the
   static 23 is retired; needs the daily-series export).
3. Checkpoint B: the sentinels sat on their baselines again 20 Aug
   (fact); the owner-approved STOP branch executes, reconciled with the
   post-rollout protocol (the 2c/2d "fallback execute at Checkpoint B"
   line from 10 Aug is superseded by the 20 Aug sequencing decision:
   they ship at ROLLOUT COMPLETION, not at the checkpoint).
4. WMT URL Submission glance (owner): the australia/legal row flipping
   to 08-20 closes the last open ledger entry.
5. Senate debate/vote watch status: resolved (passed both houses;
   Entry 3 shipped); the session records it and the assent watch
   stands.
Plus the session's cleanup list: first recovery-log entry since 25 Jul;
first radar run; portfolio d7 backfill (including reconciling the
untranscribed 20 Aug figures); the stale STATE lines flagged in the
20 Aug briefing (lapsed 08-03 audit lines, struck dispatch
instructions, WA "imminent" lines); the prediction-markets tracker
update; the lapsed-audit disposition decision.
Owner inputs remaining: the WMT glance and the GA4 glance. AI exports:
the 20 Aug brief says delivered with a window caveat; they are not yet
in the repo, so the session should start by transcribing whatever the
owner supplies.

## 8. Process notes for the next assistant

- Repo over session memory, always. The decisions log is append-only
  and is the single source of truth; when the owner's recollection and
  the repo differ, use the repo count and flag the difference (08-02
  precedent). This document was built under that rule and reports two
  unresolved contradictions rather than smoothing them (sections 2
  and 5).
- Briefings VERIFY, they do not assume. The 20 Aug briefing found a
  mid-flight spam update, a passed bill, three lapsed watches, and a
  never-run audit precisely because it re-checked every dated line
  against live sources instead of trusting STATE.
- The thrice-lapsed tripwire is now DAILY and stays daily until serving
  resumes or the owner rules otherwise. Do not let it slide back to
  weekly by habit; that is how the 18 Aug rollout was missed for two
  days.
- Owner decisions made with full knowledge are SETTLED. The
  counter-argument is logged once, marked not-to-be-re-raised, and
  never raised again. The settled list is in section 3.
- Show-me stops in a session brief are hard gates that override the
  autonomy rule for that ship. Between them, decide and report; do not
  ask mid-session.
- Verify-or-omit is never relaxed. When a primary source is
  unfetchable (parlinfo 403), the page says plainly that the figure is
  unverified and omits it; it never carries a stale figure forward
  (Entry 3 commencement-dates precedent, 20 Aug).
- Positive confirmation over absence of errors: the inverted gate's
  silent-drop failure mode, the silent 200-but-nowhere Bing failure
  mode, and the sitemap-propagation gap are all invisible-failure
  shapes this repo has learned to catch with positive checks. When you
  ship, prove the thing is THERE, live; when you dispatch, hold it
  pending until the report shows it.
- Em-dash-zero everywhere, including docs. Explicit-path staging only.
  Subagents never push. The owner reads the final report, not the
  transcript: put everything that matters in it.
